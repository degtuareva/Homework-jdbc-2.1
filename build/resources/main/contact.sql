CREATE TABLE contact (
                         id SERIAL PRIMARY KEY,
                         name VARCHAR(255),
                         surname VARCHAR(255),
                         phone_number VARCHAR(255),
                         email VARCHAR(255)
);
